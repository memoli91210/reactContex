import { createContext } from "react";

export const MonContext = createContext(null);


//1 cree le contexte(createContext)
//2 Ajouter le provider ds notre app
// 3 utiliser le contexte(useContext)